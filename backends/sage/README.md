SageMath adapter boundary.

The algebra adapter exchanges versioned JSON/protocol messages for modular,
polynomial, and matrix requests. Sage output must return facts or candidates
that are validated by UCIR before promotion.
