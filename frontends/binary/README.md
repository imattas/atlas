Binary frontend boundary.

Track 2 intake currently detects ELF artifacts and records x86-64 metadata
without executing artifact code. Instruction semantics and emulator
differential coverage are implemented behind `atlas-program` in later slices.
