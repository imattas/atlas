Track 1 e2e fixtures cover validation levels, report reproduction, cache
integrity, backend crash isolation, and CLI/Python SDK smoke paths through unit
and integration tests until a persistent runtime daemon is introduced.
